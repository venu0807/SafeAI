# SafeGuard AI ML environment configuration
# Copy to .env and fill in any secrets or overrides.
# All defaults are in config.py — uncomment here to override.

# Example: remote dataset source (if used)
# DATASET_URL=
# API_KEY=
